import React from "react"
import Layout from "../components/Layout/layout"

const contact = props => {
  return (
    <Layout>
      <p style={{ marginTop: `150px`, height: `65vh` }}>contact page</p>
    </Layout>
  )
}

export default contact
