import React from "react"
import Layout from "../components/Layout/layout"

const delivery = props => {
  return (
    <Layout>
      <p style={{ marginTop: `150px`, height: `65vh` }}>delivery page</p>
    </Layout>
  )
}

export default delivery
