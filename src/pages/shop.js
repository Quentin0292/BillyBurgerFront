import React from "react"
import Layout from "../components/Layout/layout"

const shop = props => {
  return (
    <Layout>
      <p style={{ marginTop: `150px`, height: `65vh` }}>shop page</p>
    </Layout>
  )
}

export default shop
