import React from "react"
import Layout from "../components/Layout/layout"

const offers = props => {
  return (
    <Layout>
      <p style={{ marginTop: `150px`, height: `65vh` }}>offer page</p>
    </Layout>
  )
}

export default offers
